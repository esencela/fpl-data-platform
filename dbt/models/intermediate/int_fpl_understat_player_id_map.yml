version: 2

models:
  - name: int_fpl_understat_player_id_map
    description: 'FPL -> Understat player id map for missing understat ids, mapped on best name similarity matches over a certain threshold'

    columns:
      - name: fpl_player_id
        tests:
          - not_null
          - unique

      - name: understat_player_id
        tests:
          - unique